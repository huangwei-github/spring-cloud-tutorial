spring:
  cloud:
    config:
      name: itoken-admin
      profile: dev
      discovery:
        enabled: true
        service-id: itoken-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka
server:
  port: 9004
